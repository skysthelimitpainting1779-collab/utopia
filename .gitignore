/target
**/*.rs.bk
.env
node_modules/
web/dist/
*.log
.DS_Store

# Next.js / Workflow control-plane build and local runtime output.
control-plane/.next/
control-plane/.workflow-data/
control-plane/node_modules/
control-plane/.vercel/
*.tsbuildinfo

# 运行产物（上传原文 + Tantivy 索引）。**不写前导斜杠**：`/data` 只匹配仓库根，
# 而服务在哪个目录起就在哪儿建 data/——`web/data` 就是这么漏进来的（30 个文件，
# 含两份上传的测试文档）。跟 `_bench_*` 漏掉 `_holmes2.json` 是同一类错
data/
# docs/ 是本地草稿区；只有决策记录与流水线参考入库。
# 必须写成 /docs/* 而非 /docs——父目录被忽略时，子路径无法用 ! 重新纳入。
/docs/*
!/docs/decisions/
!/docs/pipeline.md
.claude/

# 测量台与临时脚本的运行产物。**按前缀整段忽略**：
# 先前只列了 _bench_*，结果 _holmes2.json 混进了一次提交
_*.json
_*.err
_*.sse
_*.tmp
_*.out
_*.sh
_*.txt

# wiki-history 是跑测用的语料，正文与清单都不进仓库：正文约 7MB 的 CC BY-SA 文本，
# 清单是一次采样的产物。要复现就跑 fetch-wiki-history.mjs——`--manifest` 写清单，
# `--from-manifest` 按清单逐字节重建（action=parse&oldid 不可变）。
/scripts/bench/corpora/wiki-history.json
/scripts/bench/corpora/wiki-history.manifest.json
# subset-corpus.mjs 切出来的子集同理：它是清单加一行命令的产物，不是素材。
# 命令写进跑测记录，语料本身不进仓库。
/scripts/bench/corpora/wiki-*-subset.json
/scripts/bench/corpora/wiki-nov2023.json
